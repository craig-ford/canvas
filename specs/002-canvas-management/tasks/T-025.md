# specs/002-canvas-management/tasks/T-025.md

## Task: App Routing Integration

## Type
implementation

## Context
- **Phase**: Phase 6 (UI)
- **Builds on**: T-024 (Canvas Page Integration)
- **Blocks**: None (final task in chain)

## Predecessors

### Cross-Feature
| Feature | File | Task | Import Statement |
|---------|------|------|------------------|

### Within Feature
| Task | File | What This Task Imports |
|------|------|------------------------|
| T-022 | frontend/src/canvas/CanvasPage.tsx | CanvasPage component |
| T-024 | frontend/src/canvas/hooks/useCanvas.ts | useCanvas hook |

## Scope
- **MODIFY**: `frontend/src/App.tsx`

## Contract
```typescript
// Add route for /vbus/:vbuId/canvas -> CanvasPage
// Import CanvasPage and useCanvas hook
// Add to React Router configuration

const App: React.FC = () => {
  return (
    <Router>
      <Routes>
        {/* Existing routes */}
        <Route path="/vbus/:vbuId/canvas" element={<CanvasPage />} />
        {/* Other routes */}
      </Routes>
    </Router>
  );
};
```

## Logic
1. Import CanvasPage from '../canvas/CanvasPage'
2. Import useCanvas from '../canvas/hooks/useCanvas' (for type exports)
3. Add new route in Routes component
4. Route path: "/vbus/:vbuId/canvas"
5. Route element: <CanvasPage />
6. Ensure route is positioned correctly in route hierarchy
7. Verify CanvasPage receives vbuId from useParams

## Verification
- [ ] Route is added to App.tsx
- [ ] CanvasPage component is imported
- [ ] Route path matches expected pattern
- [ ] Navigation to /vbus/123/canvas renders CanvasPage
- [ ] vbuId parameter is passed correctly
- [ ] No TypeScript errors
- [ ] Route works with React Router navigation

## LOC Estimate
~5 lines