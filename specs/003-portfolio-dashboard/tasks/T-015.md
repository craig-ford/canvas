# specs/003-portfolio-dashboard/tasks/T-015.md

## Task: VBU Table Implementation

## Type
implementation

## Context
- **Phase**: Phase 6 - UI
- **Builds on**: T-010 (VBU Table Component Tests)
- **Blocks**: None
- **Requirements**: FR-005

## Predecessors

### Cross-Feature
| Feature | File | Task | Import Statement |
|---------|------|------|------------------|

### Within Feature
| Task | File | What This Task Imports |
|------|------|------------------------|
| T-010 | frontend/src/dashboard/__tests__/VBUTable.test.tsx | Test contracts for table behavior |
| T-016 | frontend/src/dashboard/HealthIndicator.tsx | `import { HealthIndicator } from './HealthIndicator'` |

## Scope
- **CREATE**: `frontend/src/dashboard/VBUTable.tsx`

## Contract
```typescript
interface VBUTableProps {
  vbus: VBUSummary[]
  onExportPDF: (vbuId: string) => void
  onViewVBU: (vbuId: string) => void
}

interface SortConfig {
  key: keyof VBUSummary
  direction: 'asc' | 'desc'
}

const VBUTable: React.FC<VBUTableProps> = ({ vbus, onExportPDF, onViewVBU }) => {
  // Sortable columns with indicators
  // Pagination with 25 items per page
  // Action buttons for each row
  // Responsive table layout
  // Accessibility attributes
}
```

## Logic
1. Create VBUTableProps interface with callback functions
2. Create SortConfig interface for column sorting
3. Implement sorting state and sort handler
4. Implement pagination state (25 items per page)
5. Build table header with sortable columns
6. Add sort indicators (↑↓) to column headers
7. Render VBU data in table rows
8. Add HealthIndicator component for status
9. Add action buttons (PDF export, view VBU)
10. Implement pagination controls
11. Add proper table accessibility attributes
12. Handle empty data state

## Verification
- [ ] Table renders with proper structure
- [ ] Column sorting works correctly
- [ ] Pagination controls function
- [ ] Action buttons trigger callbacks
- [ ] Health indicators display properly
- [ ] Accessibility attributes present

## LOC Estimate
~160 lines